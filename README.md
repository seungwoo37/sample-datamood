# DataMood Sample Repository

이 레포지토리는 `datamood` 라이브러리를 사용하는 간단한 예제를 제공합니다.  
텍스트 파일, 오디오 파일, 유튜브 URL, 뉴스/기사 URL을 한 번에 감정 분석하는 데모 스크립트를 포함합니다.

---

## 1. 사전 준비

1. Python 버전: 3.8 이상 권장
2. DataMood 라이브러리 설치

DataMood 라이브러리 레포지토리에서 다음 명령을 실행합니다.

```bash
pip install -e .
